import { forceArray } from "../utilities";

describe("Utilities Testing", () => {
    test("Ping", () => {
        expect("").toHaveLength(0).toEqual("");
    });
}); // close describe("Utilities Testing")
